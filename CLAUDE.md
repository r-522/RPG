# 双つの旅路 (Two Paths) — Design Document

このドキュメントは、本プロジェクトを継続開発するあらゆる作業者(およびAIエージェント)が、プロジェクトの設計思想を一貫して維持するための憲章です。**仕様の変更前に必ず本書を更新**してください。

---

## 1. コアコンセプト

「**勇者**」と「**魔王軍**」、2 つの陣営から好きな道を選んで進める、ブラウザ単独完結型のターン制 RPG。タイトル「**双つの旅路 (Two Paths)**」が示す通り、同じ世界・同じ最終決戦を、対立する 2 つの視点で 1 周ずつ体験する構造。

- 1 プレイ想定 10〜25 分
- セーブ不要・サーバー不要・外部依存ゼロ
- `index.html` をダブルクリックするだけで起動 (`file://` で動作)

---

## 2. ビジュアル原則

- **Canvas 2D** で全描画 (`imageSmoothingEnabled = false`)
- **論理解像度 640×480 固定**。CSS でアスペクト比を保ったまま画面サイズへフィット
- **90 年代後期 JRPG** の質感を意識:
  - 限定パレット (1 ルートあたり最大 6 色)
  - 二重枠ウィンドウ・角丸禁止・ガラスモーフィズム禁止
  - ピクセル絵キャラは `fillRect` で 10×10 グリッドを組む
  - 装備差分は色オーバーレイ / 小さなドット追加で表現
- フォントは OS 標準等幅 (`ＭＳ ゴシック` 系) のみ
- **外部アセット完全禁止**: 画像/フォント/音源ファイルは1つも使わない。音は Web Audio API で合成

絶対に採用しない美術:
- 紫⇆ピンクのSaaS風グラデーション、Neonグロー、絵文字UI、Webフォント読込

---

## 3. ゲームループ

```
タイトル → ルート選択 → プロローグ → 名前入力
   ↓
[ループ] 進軍 (オートマーチ) → エンカウント → ターン制バトル → 成長/装備
   ↓
最終決戦 → エンディング
```

### 3.1 進軍 (オートマーチ)
- 自軍と敵軍が左右から中央へ向けて自動進行
- 進行速度は **ランダム**。両者の進行度が衝突したタイミングでエンカウント発生
- 待機中は装備/特技/ステ振り/道具メニューを開ける(進行は一時停止)
- 戦闘勝利後、敵軍は後退、自軍は少し前進
- まれに「商人」イベントが発生し、装備とアイテムを売買できる
- 自軍が 100% に到達して敵軍と未会敵 → 次ゾーンへ自動進行
- 最終ゾーンで衝突 → 最終決戦

### 3.2 バトル
- ターン制コマンド: 攻撃 / 特技 / 防御 / 道具 / 逃げる
- ダメージ計算:
  ```
  damage = max(1, (atk + weaponAtk) * mul - (def + armorDef) + rand(-2, 2))
  クリティカル: 10%、× 1.6
  命中: 100% - 相手の eva%
  ```
- 防御中: 被ダメ半減 + 自分の eva +10%

### 3.3 成長
- **経験値曲線**:
  - 勇者: `floor(6  · Lv^1.40)` (緩い)
  - 魔王: `floor(11 · Lv^1.55)` (厳しい)
- 自動成長 (Lv up 毎):
  - 勇者: HP+3 MP+1 ATK+1 DEF+1 + **2pt のステ振り**
  - 魔王: HP+5 MP+2 ATK+2 DEF+1 EVA+1 (自動のみ)
- **特技習得**: Lv3 / Lv5 / Lv7 の節目で新規習得
- **装備**: 戦闘勝利時 約 28% でドロップ。商人で売買

---

## 4. バランス哲学(最重要)

**「常に拮抗していること」**を最優先する。一方的な蹂躙にも、絶望ゲーにもしない。

### 4.1 ルート非対称性
| 観点 | 勇者 | 魔王軍 |
|---|---|---|
| 基礎戦闘力 | 低い | 高い |
| 経験値の貯まりやすさ | 速い | 遅い |
| 自動成長 | 控えめ | 大きい |
| ステ振り | あり (2pt/Lv) | なし |
| エリア毎の戦闘数 | 3–5 (少なめ) | 5–7 (多め) |
| 進行ペース | 速い | 遅い |
| プレイ感 | カスタマイズ性で勝負 | 数の押し付けに耐える物量戦 |

両ルートとも **クリア所要時間がほぼ同じ** になるよう設計する。

### 4.2 ボス設計
- ボス戦は **互いのステータス ±10%** の接戦になるよう調整
- 直前のレベル目安: Lv4〜6
- 装備上振れ/下振れがあっても 「2〜4 ターン余裕勝ち or 2〜4 ターンで敗北」 のレンジに収める

### 4.3 装備のインフレ制御
- 武器: ATK +2 〜 +8 のレンジ
- 防具: DEF +1 〜 +6 のレンジ
- 商人価格はゲーム後半に向けて指数増加
- ボス HP/ATK は最強装備込みでも 4〜6 ターン戦になる値に上方修正

---

## 5. アーキテクチャ

### ファイル構成

```
RPG/
├── index.html           基本構造 + <script> 順次ロード
├── styles.css           スタイル(レスポンシブ含む)
├── CLAUDE.md            (本書)
└── js/
    ├── data.js          静的データ(パレット/敵/特技/装備/ゾーン/プロローグ)
    ├── core.js          状態/設定/音/入力/描画基盤/スプライト/フェード
    ├── battle.js        戦闘ロジック/AI/計算式
    ├── scenes.js        各シーンの update/draw
    └── main.js          メインループ起動
```

**重要**: ES Modules (`import`/`export`) は `file://` で動かない。複数 JS は **通常の `<script src=...>` を順番に並べる方式のみ**。全モジュール間でグローバル変数を共有する。

### グローバル
- `gameState`: シーン横断の状態(JSON シリアライズ可能)
- `SETTINGS`: ユーザー設定 (localStorage に永続化)
- `PALETTE` / `SKILLS` / `ENEMIES` / `ITEMS` / `WEAPONS` / `ARMORS` / `ZONES` / `PROLOGUE`: 定数

### レスポンシブ
- canvas 内部解像度 640×480 固定
- CSS: `width: min(100vw, 100vh*4/3)` で **縦横比を保ったまま画面いっぱい**にスケール
- 入力座標は `getBoundingClientRect()` で常に論理座標に正規化

---

## 6. 入力

- キーボード: ↑↓←→ / Z(決定) / X(取消)
  - キー割当は設定画面で**リバインド可能**(localStorage 保存)
- マウス: **クリック**で全UIを直接操作
- タッチ: **タップ**でクリックと同等(`pointerdown` で統一)

---

## 7. 設定

タイトル画面の「せってい」から以下を変更可能:
- **グラフィック**: スキャンライン on/off、ピクセル拡大表示 on/off
- **サウンド**: 効果音 on/off、音量 0–10
- **キーバインド**: 6 アクションを個別に再割当

全て localStorage に永続化。

---

## 8. 拡張ポイント

- `data.js` の `ZONES` / `ENEMIES` / `SKILLS` / `WEAPONS` / `ARMORS` を増やせばコンテンツ追加可能
- 新シーンは `scenes.js` に `update*` / `draw*` を追加し、`main.js` の switch に分岐を増やすだけ
- 音は `core.js` の `SE` オブジェクトに新規プリセットを追加するだけ
- セーブ機能: 既に gameState は JSON 化可能なので、`localStorage.setItem('save', JSON.stringify(gameState))` で実装可能

---

## 9. 既知の制限・非機能要件

- BGM 無し(SE のみ。BGM 追加なら Web Audio で簡易シーケンスを書く)
- マルチプレイヤー非対応
- セーブ機能未実装(状態は JSON 化可能なので追加は容易)
- ブラウザ要件: ES2020 / Canvas 2D / Web Audio API / Pointer Events (Chrome 90+, Safari 14+, Firefox 88+)

---

## 10. 開発時のチェックリスト

新機能追加時は以下を全て満たすこと:
- [ ] 90 年代 RPG 風の美意識を破っていないか (3.2 章の禁止リスト)
- [ ] `file://` で動くか (ES Modules 使ってないか)
- [ ] キーボード/マウス/タッチ全て動くか
- [ ] バランス: 拮抗を壊していないか (4 章)
- [ ] 設定の永続化(必要なら)
- [ ] 本書の更新
